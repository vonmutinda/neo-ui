diff --git a/node_modules/zustand/esm/middleware.mjs b/node_modules/zustand/esm/middleware.mjs
index 1ea9314..c0d415a 100644
--- a/node_modules/zustand/esm/middleware.mjs
+++ b/node_modules/zustand/esm/middleware.mjs
@@ -132,6 +132,7 @@ const devtoolsImpl = (fn, devtoolsOptions = {}) => (set, get, api) => {
   }
   connection.subscribe((message) => {
     var _a;
+    if (!message || typeof message !== "object") return;
     switch (message.type) {
       case "ACTION":
         if (typeof message.payload !== "string") {
@@ -172,6 +173,7 @@ const devtoolsImpl = (fn, devtoolsOptions = {}) => (set, get, api) => {
           }
         );
       case "DISPATCH":
+        if (!message.payload) return;
         switch (message.payload.type) {
           case "RESET":
             setStateFromDevtools(initialState);
diff --git a/node_modules/zustand/middleware.js b/node_modules/zustand/middleware.js
index d4f2277..4edff3d 100644
--- a/node_modules/zustand/middleware.js
+++ b/node_modules/zustand/middleware.js
@@ -134,6 +134,7 @@ const devtoolsImpl = (fn, devtoolsOptions = {}) => (set, get, api) => {
   }
   connection.subscribe((message) => {
     var _a;
+    if (!message || typeof message !== "object") return;
     switch (message.type) {
       case "ACTION":
         if (typeof message.payload !== "string") {
@@ -174,6 +175,7 @@ const devtoolsImpl = (fn, devtoolsOptions = {}) => (set, get, api) => {
           }
         );
       case "DISPATCH":
+        if (!message.payload) return;
         switch (message.payload.type) {
           case "RESET":
             setStateFromDevtools(initialState);
